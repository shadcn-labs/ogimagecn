---
title: Shadcn Registry 13
description: A clean light Open Graph image with brand logo, large headline, description, tech icons pill, and a tilted 3D preview grid on the right.
---

<ComponentPreview hideCode name="shadcn-registry-13" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @ogimagecn/shadcn-registry-13
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  name="shadcn-registry-13"
  title="components/og/shadcn-registry-13.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ImageResponse } from "next/og";

import { ShadcnRegistry13 } from "@/components/og/shadcn-registry-13";

export function GET() {
  return new ImageResponse(
    <ShadcnRegistry13
      title="Build Futuristic UIs with shadcn/ui at Warp Speed ⚡"
      description="Accelerate your project development with ready-to-use shadcn Components."
      brand="shadcn/studio"
      techIcons={["T", "S", "W", "N", "M", "R"]}
    />,
    {
      width: 1200,
      height: 630,
    }
  );
}
```

## API Reference

### ShadcnRegistry13

| Prop          | Type       |
| ------------- | ---------- |
| `title`       | `string`   |
| `description` | `string`   |
| `brand`       | `string`   |
| `techIcons`   | `string[]` |
| `logo`        | `string`   |
